# PickCast 협업 규칙

## 1. 기본 원칙

- 모든 작업은 별도 작업 브랜치에서 진행합니다.
- main에 직접 커밋하거나 푸시하지 않습니다.
- main 반영은 반드시 PR을 통해 진행합니다.
- 로컬에서 main에 직접 병합한 뒤 푸시하지 않습니다.
- 작성자가 PR을 올리고, 변경 내용을 확인한 뒤 직접 병합합니다.
- 다른 팀원의 승인은 필수가 아니며, 필요하면 리뷰를 요청합니다.

## 2. 브랜치 규칙

### 작업 방식

- 작업 전 최신 main을 받은 뒤 새 브랜치를 생성합니다.
- 사람별·파트별 고정 브랜치를 사용하지 않고 작업마다 생성합니다.
- 브랜치 하나에는 하나의 기능 또는 명확한 작업 목적을 담습니다.
- 병합이 완료된 브랜치는 삭제합니다.
- 다음 작업은 최신 main에서 새 브랜치를 생성해 진행합니다.

### 브랜치 이름

기능 개발 및 수정은 `영역/유형/작업명` 형식으로 작성합니다.

- 영역: frontend, backend, ml
- 유형: feature, fix, refactor, design, style, test 등
- 작업명: 작업 내용을 알 수 있는 영문 소문자와 하이픈(-)

| 예시 | 작업 내용 |
|---|---|
| backend/feature/event-api | 조회 이벤트 수집 API 개발 |
| backend/fix/duplicate-event | 조회 이벤트 중복 저장 수정 |
| frontend/feature/product-list | 상품 목록 화면 개발 |
| frontend/design/result-page | 결과 화면 디자인 수정 |
| ml/feature/model-training | 모델 학습 코드 작성 |
| ml/fix/session-split | 세션 구분 오류 수정 |

문서 및 프로젝트 공통 설정은 `유형/작업명` 형식을 사용합니다.

| 예시 | 작업 내용 |
|---|---|
| docs/collaboration-guide | 협업 규칙 작성 |
| docs/api-spec | API 명세 작성 |
| chore/project-structure | 초기 폴더 구조 설정 |
| ci/pr-check | PR 자동 검사 설정 |

`backend/`, `frontend/`, `ml/`은 브랜치 이름의 접두사입니다.
별도의 부모 브랜치를 만들지 않고 모두 최신 main에서 생성합니다.

## 3. 커밋 규칙

### 커밋 단위

- 하나의 기능 또는 하나의 명확한 변경 목적 단위로 커밋합니다.
- 여러 기능을 모두 구현한 뒤 한 번에 커밋하지 않습니다.
- 서로 관련 없는 기능 추가, 오류 수정, 문서 변경을 섞지 않습니다.
- 하나의 기능에 필요한 코드·테스트·관련 문서는 함께 커밋할 수 있습니다.
- 파일 개수가 아니라 변경 목적을 기준으로 커밋을 나눕니다.
- 커밋 전에 변경 내용과 포함할 파일을 확인합니다.

### 커밋 메시지

`유형: 변경 내용` 형식으로 작성합니다.

- 유형은 영문 소문자로 작성합니다.
- 콜론(:) 뒤에 한 칸 띄웁니다.
- 변경 내용은 한국어로 구체적으로 작성합니다.
- `feat`와 `feature`를 혼용하지 않고 `feature`로 통일합니다.

| 유형 | 용도 |
|---|---|
| feature | 새로운 기능 추가 |
| fix | 오류 수정 |
| refactor | 동작 변경 없이 코드 구조 개선 |
| design | 화면 디자인 변경 |
| style | 공백·들여쓰기 등 코드 형식 수정 |
| docs | 문서 작성 및 수정 |
| test | 테스트 추가 및 수정 |
| chore | 폴더 구조·의존성·환경설정 등 관리 |
| ci | 자동 검사 및 CI 설정 변경 |

좋은 예:
- feature: 상품 목록 조회 API 추가
- fix: 조회 이벤트 중복 저장 오류 수정
- refactor: 피처 계산 로직을 공통 함수로 분리
- docs: API 요청 및 응답 형식 작성

피해야 할 예:
- 작업 완료
- 최종 수정
- 이것저것 수정
- feature: 프론트 전체 구현 및 서버 수정

## 4. PR 및 병합 규칙

- 작업자는 main을 대상으로 직접 PR을 작성합니다.
- 하나의 PR에는 하나의 작업 목적을 담습니다.
- PR 제목은 커밋 메시지와 동일한 `유형: 변경 내용` 형식으로 작성합니다.
- PR에는 변경 내용, 확인 방법 및 결과를 기록합니다.
- 화면 변경이 있다면 스크린샷을 첨부합니다.
- 확인하지 못한 부분이나 남은 문제를 명시합니다.
- 작성자가 변경 사항을 확인하고 충돌을 해결한 뒤 직접 병합합니다.
- 자동 검사가 설정되어 있다면 모두 통과한 뒤 병합합니다.
- 다른 팀원의 승인은 필수가 아니며, 검토가 필요하면 리뷰를 요청합니다.
- 작업이 끝나지 않았다면 Draft PR로 등록합니다.
- 병합 후 작업 브랜치를 삭제하고 로컬 main을 최신 상태로 갱신합니다.

## 5. 공통 작업 및 파일 관리

- 같은 파일을 함께 수정할 때는 관련 팀원에게 미리 공유합니다.
- API 형식이나 공통 설정 변경은 관련 팀원에게 미리 공유합니다.
- 사용 방법이나 API가 바뀌면 관련 문서도 수정합니다.
- API 키, 비밀번호, 실제 환경변수 파일(.env)은 커밋하지 않습니다.
- 필요한 환경변수는 비밀값 없이 .env.example에 작성합니다.
- 원본 데이터, 전처리 결과, 학습 모델 파일은 Git에 올리지 않습니다.
- 데이터와 모델의 준비 방법, 버전 및 보관 위치는 문서에 기록합니다.